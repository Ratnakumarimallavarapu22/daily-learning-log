text = "programming"

result = ""
seen = set()

for char in text:
    if char not in seen:
        result += char
        seen.add(char)

print("Original:", text)
print("Without Duplicates:", result)
